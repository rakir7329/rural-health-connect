name: gramcare
description: A new Flutter project for GramCare Smart Rural Healthcare App.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  provider: ^6.1.1
  intl: ^0.20.2
  cupertino_icons: ^1.0.2
  google_fonts: 
  flutter_animate: 
  fl_chart: 
  google_nav_bar: 
  avatar_glow: 
  shared_preferences: 
  http: 
  url_launcher: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
